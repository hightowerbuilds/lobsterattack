# Daily Summary — 04-05-2026

## What got done

- Added `vercel.json` with SPA rewrites to fix page refresh breaking on production routes
- Consolidated the Hono API from 13 serverless functions down to 1 to stay within Vercel Hobby plan limits
  - Renamed `api/lib/`, `api/middleware/`, `api/routes/` to `_lib/`, `_middleware/`, `_routes/` (Vercel ignores `_` prefixed directories)
  - Updated all internal import paths
  - Added `/api/*` rewrite rule to route everything through `api/index.ts`
- Created `growth/` directory structure with `research/` and `daily-summary/` subdirectories
- Added `summary-guide.md` for agent instructions on maintaining daily summaries

## Commits

- `880a581` — Add vercel.json rewrites for SPA client-side routing
- `e16dfcd` — Consolidate API into single serverless function for Vercel Hobby plan
